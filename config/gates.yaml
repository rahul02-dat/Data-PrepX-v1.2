max_null_rate_gate:
  enabled: true
  threshold: 0.4
  per_column: true

schema_conformance_gate:
  enabled: true
  allow_extra_columns: false
  strict_dtypes: true

drift_gate:
  enabled: true
  method: psi
  psi_threshold: 0.25
  ks_p_value_threshold: 0.05
  psi_bins: 10